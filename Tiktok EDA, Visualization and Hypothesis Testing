# The purpose of this project is to conduct EDA on a TikTok data set. 
# Then and create visualizations and do hypothesis testing.

# Import packages 
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns
from scipy import stats

# Load dataset into df
data = pd.read_csv("tiktok_dataset.csv")

# Display and examine the first 5 rows
data.head()

#size of the data
data.size

#shape of the data
data.shape

#basic information about the dataset
data.info()

#descriptive statistics of dataset
data.describe()

# Create a boxplot to visualize the distribution of `video_duration_sec`
plt.figure(figsize=(5,1))
plt.title('video_duration_sec')
sns.boxplot(x=data['video_duration_sec']);

# Create a histogram
plt.figure(figsize=(5,3))
sns.histplot(data['video_duration_sec'], bins=range(0,61,5))
plt.title('Video duration histogram');

# Create a boxplot to visualize distribution of `video_view_count`
plt.figure(figsize=(5, 1))
plt.title('video_view_count')
sns.boxplot(x=data['video_view_count']);

# Create a boxplot to visualize distribution of `video_like_count`
plt.figure(figsize=(10,1))
plt.title('video_like_count')
sns.boxplot(x=data['video_like_count']);

#hypothesis testing starts here

# Check for missing values
data.isna().sum()

# Drop rows with missing values
data = data.dropna(axis = 0)

# Compute the mean `video_view_count` for each group in `verified_status' verified or not verified
data.groupby("verified_status")["video_view_count"].mean()

# Null hypothesis: There is no difference in the number of views between TikTok videos' verified accounts and unverified accounts 
# Alternative hypothesis: There is a difference in the number of views between TikTok videos posted verified accounts and posted by unverified

# Conduct a two-sample t-test to compare means and 5% significant levels chosen
not_verified = data[data["verified_status"] == "not verified"]["video_view_count"]
verified = data[data["verified_status"] == "verified"]["video_view_count"]

# Implement a t-test using the two samples.
stats.ttest_ind(a=not_verified, b=verified, equal_var=False)

output :
Ttest_indResult(statistic=25.499441780633777, pvalue=2.6088823687177823e-120)

#Since the p-value is smaller (smaller than the significance level of 5%), we reject the null hypothesis. 
#As a result, there is a statistically significant difference in the mean video view count between verified and unverified accounts on TikTok.

